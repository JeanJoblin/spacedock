import react from 'react';

export default function Selector (props = {name: "Test Name", type: "testType"}) {
  const options = Object.keys()
  return (
    props.map((object) => {
      return (
        <div value object>

        </div>
      )
    })
  ) 
}